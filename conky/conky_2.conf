-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html
--
-- Conky Lua API: https://conky.cc/lua

-- Configuration settings: https://conky.cc/config_settings
conky.config = {
    alignment = 'top_left',
    background = false,
    border_width = 0,   -- 1
    cpu_avg_samples = 2,
    default_color = 'd9ad8a', --'black', -- 'white'
    default_outline_color = 'black', -- 'white'
    default_shade_color = 'gray', -- 'white',
    double_buffer = true,
    draw_borders = true,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false, --true,
    extra_newline = false,
    font = 'FiraCode Nerd Font Mono:style=Retina:size=9',
    gap_x = 3215,
    gap_y = 0,
    minimum_height = 1, -- 5
    minimum_width = 5,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_wayland = false,
    out_to_x = true,
    own_window = true,
    own_window_class = 'Conky_2',
    own_window_type = 'normal',
    own_window_hints = 'undecorated,sticky,below,skip_taskbar,skip_pager',
    own_window_transparent = false, --true,
    own_window_argb_visual = true,
    own_window_colour = 'black',
    own_window_argb_value = 35, --192,
    show_graph_range = false,
    show_graph_scale = true,
    stippled_borders = 0,
    update_interval = 0.25,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
}

-- Variables: https://conky.cc/variables
conky.text = [[
${color purple}${nvidia modelname} $hr$color
Драйвер: ${nvidia driverversion}
Режим работы: ${nvidia perfmode}
Качество изображения: ${nvidia imagequality}

Температура процессора $hr
${nvidiagraph gputemp 36,300 green red scale -t -l}
- текущая:          ${nvidia gputemp}
- предельная:       ${nvidia gputempthreshold}
- окружающей среды: ${nvidia ambienttemp}

Тактовая частота процессора $hr
${nvidiagraph gpufreqcur 36,300 green red scale -t -l}
- текущая:      ${nvidia gpufreqcur}
- максимальная: ${nvidia gpufreqmax}
- минимальная:  ${nvidia gpufreqmin}

Тактовая частота памяти $hr
${nvidiagraph memfreqcur 36,300 green red scale -t -l}
- текущая:      ${nvidia memfreqcur}
- максимальная: ${nvidia memfreqmax}
- минимальная:  ${nvidia memfreqmin}

Скорость передачи данных в память $hr
${nvidiagraph mtrfreqcur 36,300 green red scale -t -l}
- текущая:      ${nvidia mtrfreqcur}
- максимальная: ${nvidia mtrfreqmax}
- минимальная:  ${nvidia mtrfreqmin}

Уровень производительности $hr
- текущий:      ${nvidia perflevelcur}
- максимальный: ${nvidia perflevelmax}
- минимальный:  ${nvidia perflevelmin}

Использование ресурсов $hr
- графический процессор:     ${nvidia gpuutil}%
- полоса пропускания памяти: ${nvidia membwutil}%
- видеодвигатель:            ${nvidia videoutil}%
- полоса пропускания PCIe:   ${nvidia pcieutil}%

Память $hr
${nvidiagraph memused 36,300 green red scale -t -l}
- общий объём:  ${nvidia memmax}
- используется: ${nvidia memused} ${nvidia memutil}%
- свободно:     ${nvidia memfree}

Вентилятор $hr
${nvidiagraph fanspeed 36,300 green red scale -t -l}
- скорость вращения: ${nvidia fanspeed}
- уровень:           ${nvidia fanlevel}%
]]
